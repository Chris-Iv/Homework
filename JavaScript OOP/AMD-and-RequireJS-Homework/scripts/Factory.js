define(['Container', 'Item', 'Section'], function (Container, Item, Section) {
    return {
        ListModuleElement: ListModuleElement,
        Item: Item,
        Container: Container,
        Section: Section
    };
});